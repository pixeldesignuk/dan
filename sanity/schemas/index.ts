import siteSettings from "./siteSettings";
import story from "./story";
import mission from "./mission";
import event from "./event";
import page from "./page";

export const schemaTypes = [siteSettings, story, mission, event, page];
